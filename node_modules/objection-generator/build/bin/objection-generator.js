#!/usr/bin/env node
"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const yargs_1 = __importDefault(require("yargs"));
const handle_generate_1 = require("../handle-generate");
(async () => {
    const builder = yargs => {
        return yargs
            .positional('specFile', {
            type: 'string',
            describe: 'The YAML file to use for generation.'
        })
            .positional('outDir', {
            type: 'string',
            describe: 'The directory to output generated files.'
        });
    };
    return yargs_1.default
        .wrap(yargs_1.default.terminalWidth())
        .command('generate <specFile> <outDir>', 'Generates objection.js models from a YAML file', builder, handle_generate_1.handleGenerateObjection)
        .command('knex <specFile> <outDir>', 'Generates a basic knex migration from a YAML file', builder, handle_generate_1.handleGenerateKnex)
        .options({
        templateDir: {
            type: 'string',
            description: 'Path to a custom template directory.'
        }
    })
        .demandCommand(1, '').argv;
})();
//# sourceMappingURL=objection-generator.js.map
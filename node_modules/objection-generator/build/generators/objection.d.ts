import { BaseGenerator } from './base';
export declare class ObjectionGenerator extends BaseGenerator {
    generate(): Promise<void>;
    protected generateModels(): Promise<void>;
    protected compileModel(template: HandlebarsTemplateDelegate): Generator<{
        modelName: string;
        content: string;
    }, void, unknown>;
}

import { YamlConfig } from '../interfaces';
export declare class BaseGenerator {
    defs: YamlConfig;
    templatesDir: string;
    outDir: string;
    templateFiles: string[];
    constructor(defs: YamlConfig, templatesDir: string, outDir: string);
    /**
     * Always call this first
     */
    init(): Promise<void>;
    /**
     * Call this after init
     */
    generate(): Promise<void>;
    protected prettify(content: string, filepath: any): any;
}
export declare function registerHelpers(Handlebrs: any, templatesDir: string): Promise<void>;
